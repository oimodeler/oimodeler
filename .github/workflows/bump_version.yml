name: Auto Bump Version

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  bump-version:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Bump version
        run: python bump_version.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          branch: bump-version
          title: "Auto bump version"
          commit-message: "Auto bump version"
          body: "Automatic version bump based on commit keywords"
          delete-branch: true